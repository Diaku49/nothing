# Binaries built from the Go source code
# If your main executable is named 'nothing' (based on root folder) or 'main'
/main
# If you build for windows
*.exe

# Or, if you output builds to a specific directory (recommended)
/bin/
/build/
/dist/

# Dependency directory (if you ever run 'go mod vendor')
/vendor/

# Environment variables file - VERY IMPORTANT to ignore
.env
*.env
.env.*

# Log files
*.log
logs/
*.log.*

# IDE / Editor specific files/directories
.vscode/
.idea/
*.swp
*.swo
*~
*.bak

# OS generated files
.DS_Store
Thumbs.db

# Test coverage files generated by 'go test'
coverage.out
coverage.txt
coverage.html
*.cover

# Atlas specific files (optional - check if Atlas generates local state)
# Keep atlas.hcl itself if it defines your desired schema state shared with the team.
# Add any local state or lock files Atlas might create if they shouldn't be committed.
# Example: .atlas*.sqlite

# Temporary files
/tmp/